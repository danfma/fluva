{
  "private": true,
  "name": "@fluva/root",
  "version": "1.0.0",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build",
    "publish-all": "lerna publish --skip-git",
    "test": "lerna run test"
  },
  "devDependencies": {
    "lerna": "^3.20.2",
    "typescript": "^3.8.2"
  }
}
